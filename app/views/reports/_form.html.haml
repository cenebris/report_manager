%p#notice= notice
= form_for(report) do |f|
  - if report.errors.any?
    #error_explanation
      %h2
        Błędy:
      %ul
        - report.errors.full_messages.each do |message|
          %li= message
  .field
    = f.label :description, 'Opis'
    = f.text_area :description
  .field
    = f.label :email
    = f.text_field :email
  .actions
    = f.submit 'Wyślij Zgłoszenie'